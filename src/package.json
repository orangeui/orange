{
  "name": "orange",
  "version": "1.0.0",
  "description": "cool stuff here",
  "scripts": {
    "watch": "nodemon --watch src/ --ext js,scss --exec \"npm run build\"",
    "build": "uglifyjs --compress --mangle -o ../dist/js/orange.min.js -- ./js/src/**/*.js ./js/src/*.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "modern-normalize": "^0.5.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.1",
    "uglify-js": "^3.6.0"
  }
}
