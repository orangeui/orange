{
  "name": "orange",
  "version": "1.0.0",
  "description": "cool stuff here",
  "scripts": {
    "watch": "nodemon --watch js/src/ --ext js --exec \"npm run build\"",
    "build": "uglifyjs --compress --mangle -o ../../dist/js/orange.min.js -- ./src/**/*.js ./src/*.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^1.19.1",
    "uglify-js": "^3.6.0"
  }
}
